
<Window x:Class="QuickScrcpy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuickScrcpy - 移动设备投屏工具" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SuccessButton">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DangerButton">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="35"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="TextBox" x:Key="LogTextBox">
            <Setter Property="Background" Value="#263238"/>
            <Setter Property="Foreground" Value="#AED581"/>
            <Setter Property="FontFamily" Value="Consolas, Courier New, Monospace"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="📱 QuickScrcpy" FontSize="28" FontWeight="Bold" Foreground="#2196F3" VerticalAlignment="Center"/>
            <TextBlock Text=" - 移动设备投屏工具" FontSize="14" Foreground="#666" VerticalAlignment="Bottom" Margin="5,0,0,8"/>
        </StackPanel>

        <!-- 工具栏 -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="🔍 扫描网络设备" Style="{StaticResource PrimaryButton}" 
                        Click="ScanButton_Click" x:Name="ScanButton"/>
                
                <Button Grid.Column="1" Content="⚙️ 手动连接" Style="{StaticResource PrimaryButton}" 
                        Click="ManualConnectButton_Click"/>
                
                <Button Grid.Column="2" Content="📦 安装依赖" Style="{StaticResource PrimaryButton}" 
                        Click="InstallDependenciesButton_Click" x:Name="InstallDependenciesButton" Background="#9C27B0"/>

                <ProgressBar Grid.Column="3" x:Name="ScanProgressBar" Height="5" Width="200" 
                             HorizontalAlignment="Right" Margin="10,0" Visibility="Collapsed"/>
                
                <ProgressBar Grid.Column="3" x:Name="InstallProgressBar" Height="5" Width="200" 
                             HorizontalAlignment="Right" Margin="10,0" Visibility="Collapsed" Maximum="100"/>
                
                <Button Grid.Column="4" Content="🔗 连接设备" Style="{StaticResource SuccessButton}" 
                        Click="ConnectButton_Click" x:Name="ConnectButton" IsEnabled="False"/>
                
                <Button Grid.Column="5" Content="📺 开始投屏" Style="{StaticResource SuccessButton}" 
                        Click="StartMirrorButton_Click" x:Name="StartMirrorButton" IsEnabled="False"/>
                
                <Button Grid.Column="6" Content="⛔ 断开连接" Style="{StaticResource DangerButton}" 
                        Click="DisconnectButton_Click" x:Name="DisconnectButton" IsEnabled="False"/>
            </Grid>
        </Border>

        <!-- 设备列表 -->
        <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="发现的设备列表" FontWeight="Bold" Margin="5,5,5,10" Foreground="#333"/>
                
                <DataGrid Grid.Row="1" x:Name="DeviceDataGrid" 
                          SelectionChanged="DeviceDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="设备名称" Binding="{Binding DeviceName}" Width="*"/>
                        <DataGridTextColumn Header="IP地址" Binding="{Binding IPAddress}" Width="120"/>
                        <DataGridTextColumn Header="MAC地址" Binding="{Binding MACAddress}" Width="130"/>
                        <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="50"/>
                        <DataGridTextColumn Header="制造商" Binding="{Binding Manufacturer}" Width="90"/>
                        <DataGridTextColumn Header="连接状态" Binding="{Binding IsConnected}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Text" Value="已连接"/>
                                            <Setter Property="Foreground" Value="#4CAF50"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                            <Setter Property="Text" Value="未连接"/>
                                            <Setter Property="Foreground" Value="#F44336"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Grid Grid.Row="1" x:Name="EmptyStateGrid" Visibility="Visible">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="📱" FontSize="60" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        <TextBlock Text="暂无发现的设备" FontSize="16" Foreground="#999" HorizontalAlignment="Center"/>
                        <TextBlock Text="点击上方「扫描网络设备」按钮开始扫描" FontSize="12" Foreground="#CCC" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="#E3F2FD" CornerRadius="5" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="状态: " FontWeight="Bold" Foreground="#1565C0"/>
                <TextBlock Grid.Column="1" x:Name="StatusTextBlock" Text="就绪 - 点击扫描按钮开始查找设备" Foreground="#1565C0"/>
                <TextBlock Grid.Column="2" x:Name="DeviceCountTextBlock" Text="已发现: 0 台设备" Foreground="#1565C0" FontWeight="Bold"/>
            </Grid>
        </Border>

        <!-- 日志区域 -->
        <Border Grid.Row="4" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#37474F" CornerRadius="5,5,0,0" Padding="10,5">
                    <Grid>
                        <TextBlock Text="📋 运行日志" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button Content="清空日志" HorizontalAlignment="Right" Padding="10,3" 
                                Background="Transparent" Foreground="#90A4AE" BorderBrush="#546E7A"
                                Click="ClearLogButton_Click" Cursor="Hand"/>
                    </Grid>
                </Border>

                <TextBox Grid.Row="1" x:Name="LogTextBox" Style="{StaticResource LogTextBox}"
                         Text="欢迎使用 QuickScrcpy！&#10;========================&#10;使用说明：&#10;1. 确保手机已开启「USB调试」和「网络ADB调试」&#10;2. 手机和电脑连接同一WiFi网络&#10;3. 点击「扫描网络设备」查找手机&#10;4. 选择设备后点击「连接设备」&#10;5. 连接成功后点击「开始投屏」&#10;========================&#10;"/>
            </Grid>
        </Border>
    </Grid>
</Window>
